{
  "name": "@kanban-ai/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP Server do Kanban-AI — segundo plano de controle para agents de AI externos",
  "bin": {
    "kanban-ai-mcp": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "start": "node dist/main.js",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kanban-ai/shared": "*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ws": "^8.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.2"
  }
}
