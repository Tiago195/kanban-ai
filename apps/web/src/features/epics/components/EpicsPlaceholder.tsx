/** Placeholder da feature "epics". Fundação — a UI real virá depois. */
export function EpicsPlaceholder() {
  return (
    <section className="rounded-lg border border-border bg-card p-6">
      <h2 className="text-lg font-medium">Epics</h2>
      <p className="text-sm text-muted-foreground">
        Área de epics — scaffolding. Conteúdo será implementado nas próximas fases.
      </p>
    </section>
  );
}
