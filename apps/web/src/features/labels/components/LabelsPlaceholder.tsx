/** Placeholder da feature "labels". Fundação — a UI real virá depois. */
export function LabelsPlaceholder() {
  return (
    <section className="rounded-lg border border-border bg-card p-6">
      <h2 className="text-lg font-medium">Labels</h2>
      <p className="text-sm text-muted-foreground">
        Área de labels — scaffolding. Conteúdo será implementado nas próximas fases.
      </p>
    </section>
  );
}
