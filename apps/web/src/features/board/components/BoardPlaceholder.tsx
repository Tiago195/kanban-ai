/** Placeholder da feature "board". Fundação — a UI real virá depois. */
export function BoardPlaceholder() {
  return (
    <section className="rounded-lg border border-border bg-card p-6">
      <h2 className="text-lg font-medium">Board</h2>
      <p className="text-sm text-muted-foreground">
        Área de board — scaffolding. Conteúdo será implementado nas próximas fases.
      </p>
    </section>
  );
}
