# backlog

<!-- Stories de correção derivadas do QA rodada 2 (2026-08-06). Prioridade: 🔴 crítico > 🟠 alto > 🟡 médio > 🟢 baixo -->

<!-- Épicos de adoção da memória em colmeia (memory-as-a-living-service, ADR-0027). Criados 2026-08-12 a partir da análise: a colmeia (EP-76..85) está construída mas o kanban-ai NÃO a consome, não há scheduler dos jobs e o MCP só serve stdio local. Ordem recomendada: EP-A (maior ROI) → EP-B → EP-C. -->

<!-- Épicos de melhoria derivados da análise de ferramentas de referência (Hermes Kanban PDF + Cline Kanban + Paperclip + NanoClaw), 2026-08-12. Mapa/roadmap: docs/improvements-from-reference-tools.md (fontes, rastreabilidade, itens a NÃO adotar). SPEC IMPLEMENTÁVEL por épico (estado-atual do código verificado + contratos TS/Prisma exatos + plano PR-a-PR + DOD verificável) em docs/specs/{ep-rob-robustez,ep-colab,ep-obs}.md — LEIA o spec do épico ANTES de implementar qualquer story. Ordem entre épicos: EP-ROB (maior ROI) → EP-COLAB → EP-OBS. Estes 3 épicos (M1-M12) estão em `# done`. -->

<!-- RODADA 2 de melhorias (re-análise de gap, 2026-08-12): após shipar M1-M12, os 4 sub-agents de research releram as fontes a fundo e reportaram 27 findings net-new (16 low-effort, 11 medium). Consolidados em 6 novos épicos abaixo (EP-BLOCK, EP-CTX, EP-SCHED, EP-OBS2, EP-BUX, EP-HARD). Mapa/rastreabilidade: docs/improvements-from-reference-tools.md §7-8 (rodada 2). Cada finding validado contra os 8 invariantes + lista de veto. 2 itens DEFER (Routines cron/webhook = módulo grande; Egress lockdown = conflita ADR-0019). NENHUM implementado ainda — só planejado. Ordem recomendada: EP-BLOCK → EP-CTX (maior ROI no loop) → EP-BUX → EP-HARD → EP-OBS2 → EP-SCHED. -->

- [ ] **🟡 US-CLONE-PROGRESS — Progresso granular (%/objetos) da clonagem de Project em tempo real** *(derivado da exploração 2026-08-13: hoje a UI só mostra o estado coarse `pending→cloning→ready|failed` via WS `project.clone_state`; durante `cloning` o badge fica estático, sem % nem contagem de objetos — igual ao que o `git clone` mostra no terminal)*
  - **Sintoma:** ao clonar um repo grande, o usuário vê o badge "clonando" parado e não sabe se travou ou quanto falta. O `git clone` de terminal mostra `Receiving objects: 42% (420/1000), 1.2 MiB | 800 KiB/s` — queremos paridade disso na UI.
  - **Estado-atual verificado (código):** `ProjectWorkspaceService.doClone` (apps/api/src/modules/projects/project-workspace.service.ts) chama `git.clone` (isomorphic-git) **SEM `onProgress`** no modo https, e `systemGitClone` usa `execFileAsync('git', ['clone', ...])` **sem `--progress`/parse de stderr** no modo ssh. Não há dado de byte/percent/fase em lugar nenhum — só as 3 transições do enum `ProjectCloneState`. O evento WS `ProjectCloneStateEvent` (packages/shared/src/events.ts) carrega `{projectId, state, error?}`. `RealtimeService.broadcast` (fan-out real) já funciona; `useRealtime.ts:158` já reage invalidando React Query.
  - **Escopo proposto (PR-a-PR):**
    1. **Backend https (isomorphic-git):** passar `onProgress: (p) => ...` ao `git.clone` — `p` traz `{phase, loaded, total}`. Throttle (~250–500ms ou por Δ%) para não floodar o WS.
    2. **Backend ssh (systemGit):** adicionar `--progress` ao `git clone`, trocar `execFileAsync` por `spawn` e parsear o **stderr** linha a linha (regex das fases `Counting/Compressing/Receiving/Resolving objects`) → mesmo shape `{phase, loaded, total}`.
    3. **Contrato compartilhado:** novo evento WS `ProjectCloneProgressEvent` (`type:'project.clone_progress'`, `{projectId, phase, loaded, total, percent}`) em packages/shared/src/events.ts + união `ServerEvent` (atualizar api **e** web — invariante type-safe). NÃO sobrecarregar `ProjectCloneStateEvent` (semânticas distintas: transição discreta vs progresso contínuo).
    4. **Frontend:** `useRealtime.ts` guarda o progresso mais recente por `projectId` (state leve, sem invalidar React Query a cada tick — só o `clone_state` final invalida). `CloneStateBadge` (ProjectsManager.tsx + ProjectExplorer.tsx) mostra barra/percent + fase enquanto `state==='cloning'`; ao chegar `ready`/`failed`, volta ao badge normal.
  - **Invariantes/cuidados:** progresso é **efêmero** (não persistir em `Project` — só WS transiente); throttle obrigatório; se `total` for desconhecido (fase Counting), mostrar spinner indeterminado; não vazar credencial em nenhuma linha de stderr logada. `git.clone` do isomorphic-git só reporta progresso das fases que ele executa (download); para repos ssh a fidelidade vem do `git` do sistema.
  - **DOD:** durante o clone (https **e** ssh) a UI mostra %/fase/objetos atualizando em tempo real via WS; ao concluir, badge vira `pronto`; ao falhar, `falhou` + `lastError`; progresso não é persistido; contrato type-safe nos 2 lados; build+lint+test verdes.

<!-- ⚠️ Bloqueio observado de clone ssh (2026-08-13, investigado sob pedido do usuário) — NÃO é bug de código, é config de ambiente. Repo `git@github.com:uol-universo-online/uolcs-host-...-provisioner-api.git` falha ao clonar por DOIS motivos empilhados: (1) `PROJECTS_ALLOW_SSH=false` (default no docker-compose.yml) → o clone é REJEITADO no guard `assertSshAllowed` antes de tentar, com erro claro mencionando `PROJECTS_ALLOW_SSH=true`; (2) mesmo habilitando a flag, o container `kanban-ai-api` NÃO tem `/root/.ssh/` (nenhuma chave montada) → `ssh -T git@github.com` dentro do container volta VAZIO (sem identidade), logo não autentica no repo PRIVADO da org. No HOST o `git ls-remote` do mesmo repo funciona (a chave do host tem acesso). CORREÇÃO de ambiente (não requer código): setar `PROJECTS_ALLOW_SSH=true` E montar a chave ssh no container (ex.: volume `~/.ssh:/root/.ssh:ro` + `known_hosts`, ou ssh-agent forwarding), OU usar URL https com credencial (`authKind` https + PAT via `onAuth`). Reavaliar se vale uma story de "montar ssh no container" separada. -->

# in progress

<!-- Itens vagos/bloqueados abaixo permanecem parkeados (aguardam clarificação ou conflitam com ADR aceito) — não são parte de nenhum épico ativo. -->

- [ ] Precisamos melhorar o chat de conversa do backlog-chat
  - ⚠️ **Bloqueado (aguarda clarificação):** item vago, sem sintoma nem critério de aceite. O que melhorar? (UX/layout, streaming de resposta, contexto injetado no prompt, persistência do histórico, latência?) Não é implementável "1 a 1" sem escopo definido pelo usuário.

- [ ] tentar disponibilizar tudo em docker
  - ⚠️ **Bloqueado (conflita com ADR-0019, aceito):** a API precisa rodar no **host** porque o loop engine dá `spawn` no Copilot CLI com `cwd` = git worktree dentro do repo-alvo (`aiProject`), que é um caminho arbitrário do FS do usuário; dentro do Docker a API só enxerga `/app`. O `docker-compose.yml` já expõe o profile opt-in `docker-app` para subir Nest+Vite em container (dev do próprio framework). Não há ação segura sem violar o ADR — reabrir só se o worktree isolado deixar de ser stub e o alvo for montável.

# done
<!-- apenas ultimas 2 tarefas, para n poluir o arquivo -->

- [x] **🟢 EP-SCHED — Agendamento & wakes diferidos** *(Paperclip #3/#4 — escopo = só US-SCHED1; US-SCHED2 permanece DEFER)* — **CONCLUÍDO 2026-08-13 (build shared→api/mcp/web verde exit 0; lint 0 erros/26 warnings pré-existentes; 264 specs ai-engine serial verdes 0 fail/0 skip — 11 do `wakeup-monitor.spec` net-new incl. 4 white-box dirigindo o `Orchestrator.fireDueMonitors` real + regressão wakeup-queue/orchestrator-guards/claim-lease/stuck-sla; `prisma validate` ok + `migrate status` in sync = 30 migrations; smoke empírico host (:3399, AGENT_WAKEUP_QUEUE_ENABLED=true): `POST /cards/:id/loop/monitor` → `{scheduled:true}` + row `WakeupQueue{reason:monitor_due,status:pending,scheduledFor:+30min,notes,maxAttempts:3}` persistida; `DELETE .../loop/monitor` → `{cleared:true}` + row `status:done` (one-shot))**
  - **Validação (2026-08-13) contra o código real:** o `WakeupQueue` durável já existe (US-COLAB3) MAS é event-driven & imediato (`enqueueWakeup`→`onStoryEnterInProgress`) — SEM `scheduledFor`/time-gating e SEM tick que faça poll de tempo futuro (`listPending` estava sem filtro de tempo e não era chamado). Logo o wake DIFERIDO no `nextCheckAt` é genuinamente NET-NEW e adotável. Reaproveita a tabela `WakeupQueue` (+ coluna aditiva), o enum `WakeupReason`, o padrão de tick periódico (`startClaimSweep`/`startNoCommentStreakScan`) e `onStoryEnterInProgress` como mecanismo canônico de wake.
  - **DOD do épico:** ✅ issue monitors one-shot diferidos, durable (sobrevivem restart em Postgres); ✅ dispara no `nextCheckAt`, one-shot clear, re-arm e auto-clear em terminal provados por specs determinísticos; ✅ arm/clear via HTTP + tool MCP, type-safe nos dois lados; ✅ migration aditiva. Build+lint+test verdes.
  - [x] **US-SCHED1 — Issue monitors: one-shot deferred wake** *(Paperclip #4)* — med — **Schema aditivo:** `WakeupQueue` += `scheduledFor DateTime?` (null = imediato, comportamento legado preservado) + `notes`/`timeoutAt`/`maxAttempts` + `@@index([scheduledFor])` (migration `sched1_wakeup_scheduled_for`; **colunas dedicadas** escolhidas sobre `payload Json` por simplicidade/type-safety/query). **Enum:** `monitor_due` adicionado a `WAKEUP_REASON` (packages/shared, consumido por api+web). **Serviço:** `WakeupQueueService.enqueueDeferred()` (coalescing preservado), `listDue(now)` (time-gate: `scheduledFor` null OU `<= now`), `clearMonitor(storyId)` (one-shot). **Orchestrator:** `startMonitorTick` (espelha `startClaimSweep`: `setInterval`+`unref`+try/catch por job, cadência `watchdogIntervalMs`, gated por `wakeupEnabled`), `fireDueMonitors(now)` (drena due `monitor_due`→`onStoryEnterInProgress`→clear), `setMonitor`/`clearMonitor`, auto-clear em terminal (`finishAuto`). **API:** `POST`/`DELETE /cards/:id/loop/monitor` (Zod `setMonitorSchema{nextCheckAt,notes?,timeoutAt?,maxAttempts?}`). **MCP:** tools `loop_set_monitor`/`loop_clear_monitor` (cliente HTTP fino, ADR-0020). Reusa flag `wakeupQueueEnabled` (sem novo config). 11 specs (7 fila + 4 white-box orchestrator com `now` injetado — **sem fake timers flaky**).
  - ~~US-SCHED2 — Routines: cron/webhook/API triggers~~ *(Paperclip #3)* — ⏸️ **DEFER (não implementado):** entidade `Routine` + `routine_triggers` (schedule cron / webhook HMAC / api) + `concurrencyPolicy`/`catchUpPolicy` é um módulo novo inteiro. Push-driven poderoso (CI-triggered rework, stand-up diário) mas fora de escopo do v1. Reavaliar SÓ após US-SCHED1 provar o scheduler tick em produção. Não implementar sem re-priorização explícita.


- [x] **🟡 EP-OBS2 — Observabilidade rodada 2 (event log, cheap recovery, tracing, anomaly)** *(Paperclip #2/#7/#9/#10 + Hermes N9 — escopo revisado 5→4)* — **CONCLUÍDO 2026-08-13 (build all-workspaces verde exit 0; lint 0 erros/26 warnings pré-existentes; 48 specs net-new serial (OBS2-2=6, OBS2-1=6, OBS2-5=7, OBS2-4=29) + 62 regressão (cards.service/orchestrator-guards/claim-lease/stuck-sla/review) = 110 verdes; `prisma validate` ok + `migrate status` in sync (29 migrations); smoke empírico host: `/health`→status:ok, boot limpo SEM atividade OTel quando endpoint unset (zero overhead confirmado), `PATCH /cards/:id/move` → `CardEvent{kind:card_moved}` persistido e retornado por `GET /cards/:id/events`)**
  - **Validação (2026-08-13, 5 agents read-only) reduziu 5→4 stories:** OBS2-2 KEEP (100% net-new); OBS2-5 KEEP/retarget (override de modelo por-spawn já existe via `COPILOT_MODEL`, mas sem tier "cheap"); OBS2-1 TRIM (OTel net-new, mas trace end-to-end super-promete — trabalho real roda em subprocessos Copilot que a API não traça; reduzido a tracing opt-in só da API); OBS2-4 TRIM (long-active=redundante c/ watchdog, high-churn=redundante c/ anti-thrash — só no-comment streak é net-new); **OBS2-3 DROP/DEFER** (premissa "soma de providers mistos engana" FALSA hoje — só 1 adapter faturável + mock, sem rastreio de provider; ledger completo prematuro → dobrado como provenance mínimo `provider` dentro do OBS2-5).
  - **DOD do épico:** ✅ `CardEvent` append-only + tail por id + tool MCP; ✅ recovery status-only com modelo cheap (guard `allowDeliverableWork:false`) + provenance `provider` na telemetria; ✅ OTel opt-in só-API; ✅ no-comment streak vira ação de review rate-limited/snooze-aware. Build+lint+test verdes; contratos type-safe nos dois lados; migrations aditivas.
  - [x] **US-OBS2-2 — Typed append-only event log (`CardEvent`) + tail + MCP** *(Hermes N9)* — KEEP full — modelo `CardEvent` (`kind`, `payload JSON`, `ts`, `@@index([cardId, ts])`, migration aditiva `obs2_card_event_log`); `emitCardEvent` + `listEvents(cardId,{since,limit})` em `cards.service.ts`, emitido nas 5 transições; endpoint `GET /cards/:id/events?since=&limit=`; tool MCP `list_card_events` (cliente HTTP fino, ADR-0020); contrato `packages/shared/src/card-events.ts` (CARD_EVENT_KINDS/CardEventKind/CardEventDTO). Convive com `Activity`. 6 specs. **Verificado empírico: card_moved emitido→persistido→tail.**
  - [x] **US-OBS2-5 — Cheap recovery lane + usage provenance** *(Paperclip #2 + #10 mínimo)* — KEEP/retarget (+ absorve OBS2-3) — `recovery-lane.ts` (puro `resolveDispatchModel`/`recoveryGuardLines`); `runIteration(opts{recovery})` (flag interno, sem novo WakeupReason) seleciona `agent.cheapModelId` quando setado + injeta guard "⚠️ Recovery status-only: NÃO produza trabalho entregável" scrubbed dos continuations normais; wired em `recoverStuckStory`. **Provenance:** `AgentRunResult.provider?` (copilot/mock runners setam) → `Iteration.provider String?` (migration `obs2_iteration_provider`). Sem `CostEvent`/`executionSegments`. 7 specs.
  - [x] **US-OBS2-1 — OTel tracing opt-in (só-API)** *(Paperclip #9)* — TRIM — `shared/observability/tracing.ts` (`initTracing()` + puro `resolveTracingConfig()`); lazy dynamic `import()` só quando `OTEL_EXPORTER_OTLP_ENDPOINT` setado (pacotes nunca carregados quando off = zero overhead); auto-instr HTTP/Fastify/PG, `fs/dns/net` off; pacote ausente = 1 log + segue; init ANTES do `NestFactory.create` em `main.ts`. **Não promete trace até o subprocesso Copilot.** +5 deps OTel. 6 specs. **Verificado empírico: boot SEM atividade OTel quando off.**
  - [x] **US-OBS2-4 — No-comment streak review action** *(Paperclip #7 — só 1 gatilho)* — TRIM — `no-comment-streak.ts` (puro: iteração tem sinal se `summary`|`handoffNextStep` não-vazio OU needsHuman; streak = trailing consecutivos sem sinal; anomalous ≥ threshold, ≤0 desliga); scan no tick do watchdog existente (`scanNoCommentStreaks`); `ReviewAction` model (migration `obs2_review_action`) + `review-action.{logic,service}.ts` (rate-limit/snooze puro + persistência) + endpoints snooze/list + `ReviewActionFlaggedEvent` (WS). Não move/cancela story. **DROP** long-active + high-churn (redundantes). 29 specs (15+10+4).
  - ~~US-OBS2-3 — Cost ledger com biller/billingType/executionSegments~~ *(Paperclip #10)* — **DROP/DEFER:** premissa "soma de providers mistos engana" FALSA hoje (1 adapter faturável + mock, sem rastreio de provider); `CostEvent`/`executionSegments`/`cheap_preflight` não existem no código. Provenance mínimo (`provider`) absorvido pelo OBS2-5; ledger completo reavaliar SÓ quando existir 2º adapter faturável.

